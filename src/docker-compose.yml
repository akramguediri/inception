version: "3.7"

volumes:
  wordpress_v:
    driver: local
    driver_opts:
      type: none
      device: /home/aguediri/data/wordpress
      o: bind
  mariadb_v:
    driver: local
    driver_opts:
      type: none
      device: /home/aguediri/data/mariadb:/var/lib/mysql
      o: bind

services:
  mariadb:
    build: ./requirements/mariadb/
    image: mariadb
    container_name: mariadb
    restart: always
    env_file:
      - .env
    ports:
      - "3306:3306"
    volumes:
      - mariadb_v:/var/lib/mysql
    networks:
      - inception

  nginx:
    build: requirements/nginx/.
    image: nginx
    container_name: nginx
    ports:
      - "443:443"
    restart: always
    env_file:
      - .env
    volumes:
      - wordpress_v:/var/www/html
    depends_on:
      - wordpress
    networks:
      - inception

  wordpress:
    build: requirements/wordpress/.
    image: wordpress
    container_name: wordpress
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - wordpress_v:/var/www/html
    networks:
      - inception
    depends_on:
      - mariadb

networks:
  inception:
    name: inception
    driver: bridge
